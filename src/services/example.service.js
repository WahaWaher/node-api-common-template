/**
 * Service "Example"
 */
module.exports = {
  // Checks service is available and ready to use (check it before init app)
  check() {},
  // Gets information (for example, from third-party API)
  get() {},
  // Other data/methods...
};
